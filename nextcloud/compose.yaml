version: '3.8'

services:
  nextcloud-mariadb:
    image: mariadb:10.11.2
    container_name: nextcloud-mariadb
    restart: always
    command: --transaction-isolation=READ-COMMITTED --binlog-format=ROW
      --skip-innodb-read-only-compressed=OFF
    volumes:
      - se-nextcloud-mariadb:/var/lib/mysql
    environment:
      - MARIADB_AUTO_UPGRADE=1
      - MARIADB_INITDB_SKIP_TZINFO=1
      - MYSQL_TCP_PORT=15100
      - MYSQL_UNIX_PORT=15200
      - MARIADB_ALLOW_EMPTY_ROOT_PASSWORD=true
      - MYSQL_USER=nextcloud
      - MYSQL_PASSWORD=qwerty
      - MYSQL_DATABASE=nextcloud
    networks:
      - nextcloud
    logging:
      driver: "json-file"
      options:
        max-size: "1024m"
        max-file: "3"
        tag: "{{.Name}}"

  nextcloud:
    image: nextcloud:25.0.4-fpm-alpine
    container_name: nextcloud
    restart: always
    volumes:
      - se-nextcloud:/var/www/html
      - ./php-fpm.conf:/usr/local/etc/php-fpm.d/www.conf
      - ./zz-docker.conf:/usr/local/etc/php-fpm.d/zz-docker.conf
    environment:
      - OVERWRITEPROTOCOL=https
      - MYSQL_PASSWORD=qwerty
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_HOST=nextcloud-mariadb:15100
    depends_on:
      - nextcloud-mariadb
    networks:
      - nextcloud
    logging:
      driver: "json-file"
      options:
        max-size: "1024m"
        max-file: "3"
        tag: "{{.Name}}"

  nextcloud-nginx:
    image: nginx:1.23.3
    container_name: nextcloud-nginx
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    volumes_from:
      - nextcloud
    networks:
      - default
      - nextcloud
    depends_on:
      - nextcloud
    logging:
      driver: "json-file"
      options:
        max-size: "1024m"
        max-file: "3"
        tag: "{{.Name}}"
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.nextcloud-nginx.loadbalancer.server.port=15000"
      - "traefik.http.routers.nextcloud-nginx.rule=Host(`cloud.salvatoreemilio.\
        it`) || Host(`www.cloud.salvatoreemilio.it`)"
      - "traefik.http.routers.nextcloud-nginx.tls.certresolver=myresolver"
      - "traefik.http.routers.nextcloud-nginx.entrypoints=websecure"
      - "traefik.docker.network=se-default"

  nextcloud-cron:
    image: nextcloud:25.0.4-fpm-alpine
    container_name: nextcloud-cron
    restart: always
    volumes:
      - se-nextcloud:/var/www/html
    entrypoint: /cron.sh
    depends_on:
      - nextcloud-mariadb
    networks:
      - nextcloud
    logging:
      driver: "json-file"
      options:
        max-size: "1024m"
        max-file: "3"
        tag: "{{.Name}}"

volumes:
  se-nextcloud-mariadb:
    external: true
  se-nextcloud:
    external: true

networks:
  default:
    external: true
    name: se-default
  nextcloud:
    external: true
    name: se-nextcloud
